### EAS CLI
Подробная инструкция
#### 1. Установи EAS CLI

````bash
npm install -g eas-cli
````
И залогинься:
````bash
eas login
````
#### 2. Сгенерируй ключ подписи (keystore)
Expo может сделать это автоматически, но можно вручную через keytool:
````bash
keytool -genkeypair -v -storetype PKCS12 -keystore my-upload-key.keystore \
  -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
````
Это создаст файл my-upload-key.keystore, который будет использоваться для подписи.

#### 3. Настрой eas.json
В корне проекта создай/открой eas.json и добавь профиль для .apk:
````json
json
{
  "build": {
    "production": {
      "android": {
        "buildType": "apk"
      }
    }
  }
}
````
⚠️ По умолчанию Expo собирает .aab. Чтобы получить .apk, нужно явно указать buildType: "apk".

#### 4. Запусти сборку
````bash
eas build -p android --profile production
````
После завершения сборки ты получишь .apk файл, подписанный твоим ключом.

#### 5. Управление ключами
Если хочешь использовать существующий ключ (например, из Google Play Console), можно настроить его через:

````bash
expo credentials:manager
````
Там можно загрузить свой .keystore и задать пароль, alias.
🔑 Важные моменты
.aab нужен для публикации в Google Play, но .apk удобен для тестов и установки напрямую.

Подписанный .apk обязателен для публикации — без ключа Google Play не примет сборку.

Expo рекомендует использовать EAS Build, так как старый expo build:android устарел.
